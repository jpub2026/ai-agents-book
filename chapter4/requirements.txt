# =================================================
# Chapter 4 전용 requirements
# 루트의 requirements.txt 와 버전을 맞춘 것이므로
# 루트에서 `pip install -r requirements.txt` 를 이미 실행했다면
# 이 파일을 별도로 실행할 필요가 없습니다.
# =================================================

# LangChain 0.3 계열
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# 타입 검증과 데이터 모델링
pydantic>=2.8.0

# 하이브리드 검색 (4.3.2절)
sentence-transformers>=2.2.2
rank-bm25>=0.2.2

# 검색 도구 (Chapter 4에서 사용)
duckduckgo-search>=6.0.0

# HTTP / LLM provider
requests>=2.31.0
openai>=1.0.0
